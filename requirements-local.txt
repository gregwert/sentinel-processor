# Local (non-Docker) dependencies
# rasterio's Windows pip wheel bundles GDAL internally — no system GDAL install needed.
# Use opencv-python (not headless) for local desktop installs.

rasterio==1.3.10
numpy==1.26.4
opencv-python==4.9.0.80
scikit-image==0.23.2
scipy==1.13.0
streamlit==1.35.0
tornado==6.2.0
Pillow==10.3.0
tqdm==4.66.4
pyyaml==6.0.1
