# Geospatial — GDAL is provided by the osgeo/gdal base image in Docker.
# rasterio's pip wheel bundles GDAL internally for local/Windows installs.
# Either way, do NOT add a separate gdal package here.
rasterio==1.3.10
numpy==1.26.4

# Image processing
# opencv-python-headless avoids Qt/display dependencies on both Docker and local Windows.
opencv-python-headless==4.9.0.80
scikit-image==0.23.2
scipy==1.13.0

# Frontend / async server
streamlit==1.35.0
tornado==6.2.0

# Utilities
Pillow==10.3.0
tqdm==4.66.4
pyyaml==6.0.1
